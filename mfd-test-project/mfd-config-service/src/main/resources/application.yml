server:
  port: 8888
spring:
  application:
    name: config-service
  profiles:
      active: native
  cloud:
    config:
      profile:  dev
      label: master
      server:
        native:
          searchLocations:  classpath:/config